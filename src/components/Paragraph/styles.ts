import { StyleSheet } from 'react-native';

import light from '~/styles/theme/light';

export const styles = StyleSheet.create({
  paragraph: {
    fontFamily: light.fontFamily.ptSerifRegular,
    fontSize: 14,
    lineHeight: 21,
    letterSpacing: 1,
  },
});
